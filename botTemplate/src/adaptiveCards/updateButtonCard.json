{
    "type": "AdaptiveCard",
    "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
    "version": "1.5",
    "body": [
        {
            "type": "ActionSet",
            "actions": [
                {
                    "type": "Action.Submit",
                    "title": "Update Card",
                    "data":{
                        "actionType" : "update",
                        "botId" : "${botId}",
                        "groupId" : "${groupId}"
                    }
                }
            ]
        }
    ]
}